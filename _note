#compdef _note note

function _note {
    local line

    _arguments -C \
        "-l[List all notes]" \
        "-d[Delete a note]" \
        "-p[Convert a note to PDF]" \
        "*::arg:->args"

    _files -W "$HOME/.notes" -g '*.md(:r)'
} 
